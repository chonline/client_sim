// WzInt is a header-only class with trivial implementation. This translation
// unit exists so CMake has something to compile and so the linker never
// complains about missing symbols if the header is refactored later.

#include "wzlib/WzInt.h"
