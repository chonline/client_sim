# tools: command-line utilities built on top of wzlib
#
# Currently only wzdump, which verifies that wzlib can read a real WZ
# file end-to-end. More tools (e.g. wzgrep, wzextract) can be added.

add_subdirectory(wzdump)
